{
    "name": "rpgmem/ffcertificate",
    "description": "Free Form Certificate — WordPress plugin for dynamic forms, PDF certificates, and appointment scheduling.",
    "type": "wordpress-plugin",
    "license": "GPL-3.0-or-later",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "yoast/phpunit-polyfills": "^2.0",
        "brain/monkey": "^2.6"
    },
    "autoload": {
        "psr-4": {
            "FreeFormCertificate\\": "includes/"
        },
        "classmap": [
            "includes/class-ffc-autoloader.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "FreeFormCertificate\\Tests\\": "tests/"
        }
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        },
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit",
        "test:unit": "phpunit --testsuite=unit"
    }
}
