/**
 * FFC Self-Scheduling Calendar Admin
 *
 * Admin-side scripts for the self-scheduling calendar system.
 *
 * @since 4.1.0
 * @package FreeFormCertificate\SelfScheduling
 */

(function($) {
    'use strict';

    const FFCCalendarAdmin = {

        init: function() {
            this.bindEvents();
        },

        bindEvents: function() {
            // Self-scheduling admin event bindings
        }
    };

    $(document).ready(function() {
        FFCCalendarAdmin.init();
    });

})(jQuery);
