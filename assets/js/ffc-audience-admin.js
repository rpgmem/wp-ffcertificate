/**
 * FFC Audience Admin
 *
 * Admin-side scripts for the audience scheduling system.
 *
 * @since 4.5.0
 * @package FreeFormCertificate\Audience
 */

(function($) {
    'use strict';

    const FFCAudienceAdmin = {

        init: function() {
            this.bindEvents();
        },

        bindEvents: function() {
            // Audience admin event bindings
        }
    };

    $(document).ready(function() {
        FFCAudienceAdmin.init();
    });

})(jQuery);
